<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.utilsandviews">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!--移动网络检测所需-->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /><!--Wifi网络检测所需-->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" /><!--启用无线网络权限-->
    <uses-permission android:name="android.permission.INTERNET" /><!--网络权限，检查wifi是否可用所需-->

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>